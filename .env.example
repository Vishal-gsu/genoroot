# Optional. Not required — the tap-through intake is the product.
# Not set up or tested on this submission.
# If added later: “type what you’ve tried” on treatments (Q12) → POST /api/extract
GEMINI_API_KEY=
